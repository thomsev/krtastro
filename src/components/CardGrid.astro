---
const { items, title } = Astro.props;
---

<section class="section">
  <div class="container">
    {title && <h2 data-animate="fade-up">{title}</h2>}
    <div class="grid cols-3">
      {
        items.map((item: any) => (
          <article class="card" data-animate="fade-up">
            {item.icon && (
              <div class="card-icon" aria-hidden="true" set:html={item.icon} />
            )}

            {item.image && (
              <div class="card-media">
                <img
                  src={item.image}
                  alt={item.imageNote ? `Illustrasjon for ${item.title ?? item.name}` : item.title ?? item.name}
                  loading="lazy"
                />
                {item.imageNote && <span class="image-note">{item.imageNote}</span>}
              </div>
            )}

            <h3>{item.title ?? item.name}</h3>
            {item.location && (
              <p>
                <strong>{item.location}</strong>
              </p>
            )}

            {item.description && <p>{item.description}</p>}

            {item.note && <p>{item.note}</p>}

            {item.website && (
              <p>
                <a href={item.website} target="_blank" rel="noreferrer">
                  Besøk nettside
                </a>
              </p>
            )}
          </article>
        ))
      }
    </div>
  </div>
</section>
